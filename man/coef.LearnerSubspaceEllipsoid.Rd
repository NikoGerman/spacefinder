% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef_LearnerSubspaceEllipsoid.R
\name{coef.LearnerSubspaceEllipsoid}
\alias{coef.LearnerSubspaceEllipsoid}
\title{Extract coefficients from fitted ellipsoid learner}
\usage{
\method{coef}{LearnerSubspaceEllipsoid}(object, ...)
}
\arguments{
\item{object}{A \code{LearnerSubspaceEllipsoid} object with fitted subspace parameters}

\item{...}{Additional arguments (currently unused)}
}
\value{
A \code{data.table} with columns:
\itemize{
\item \code{hyperparameters}: List column containing hyperparameter names
\item \code{A}: List column of positive definite matrices defining ellipsoid
shape and orientation
\item \code{b}: List column of translation vectors
\item \code{cat_hp}: Categorical level (only if task has categorical hyperparameters)
}
}
\description{
Extracts fitted subspace parameters from a trained \code{LearnerSubspaceEllipsoid}
object. Returns the transformation matrix \eqn{A} and translation vector \eqn{b}
that define the fitted ellipsoid.
}
\details{
The ellipsoid is defined by \eqn{\|Ax + b\|_2 \leq 1} where:
\itemize{
\item \eqn{A \in \mathbb{R}^{p \times p}} is a positive definite matrix
\item \eqn{b \in \mathbb{R}^p} is the translation vector
\item \eqn{x} are points inside the ellipsoid in original coordinates
}

The center of the ellipsoid is: \eqn{c = -A^{-1}b}

The semi-axes lengths and orientations are determined by the eigendecomposition
of \eqn{A^{-1}}: if \eqn{A^{-1} = V\Lambda V^T}, then the semi-axes have lengths
\eqn{\sqrt{\lambda_i}} in directions given by columns of \eqn{V}.

When the task includes categorical hyperparameters, separate coefficient sets
are returned for each categorical level, identified by the \code{cat_hp} column.
}
\section{Error Handling}{

Throws an error if the learner has not been trained. Call \code{train()} before
extracting coefficients.
}

\examples{
\dontrun{
# Train learner
task <- TaskSubspace$new(data, target_measure = "auc",
                         hps = c("learning_rate", "max_depth"))
learner <- LearnerSubspaceEllipsoid$new(task)
learner$train(q_val = 0.9, lambda = 0.1)

# Extract coefficients
coefs <- coef(learner)
print(coefs$A[[1]])  # Shape matrix
print(coefs$b[[1]])  # Translation vector

# Compute ellipsoid center
A <- coefs$A[[1]]
b <- coefs$b[[1]]
center <- -solve(A) \%*\% b

# Compute semi-axes
A_inv <- solve(A)
eigen_decomp <- eigen(A_inv)
semi_axes <- sqrt(eigen_decomp$values)
directions <- eigen_decomp$vectors
}

}
\seealso{
\code{\link{LearnerSubspaceEllipsoid}} for the learner class.
\code{\link{coef.LearnerSubspaceBox}} for the axis-aligned variant.
\code{\link{coef.LearnerSubspacePolygon}} for the oriented hyperrectangle variant.
}
